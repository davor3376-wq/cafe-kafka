Cafe Kafka — Vite + React + TypeScript + Tailwind project
This project was generated from your Figma export.

Location: /mnt/data/cafe-vite

How to run locally:
1. cd cafe-vite
2. npm install
3. npm run dev
Then open http://localhost:5173

Notes:
- Image assets from your Figma export were copied into src/assets/
- Vite alias figma:asset -> /src/assets is configured in vite.config.ts
- If any imports fail, check src/components/* for references and adjust paths.
